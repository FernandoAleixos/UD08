package ejercicios;

public class ejercicio5 {
    /*
    * 1. Se dice que instanciamos una clase cuando creamos objetos de dichaclase. Se dice que instanciamos una clase cuando creamos una subclase dedicha clase.
    * TRUE, FALSE
    *
    * 2. Si una clase es abstracta no se puede instanciar. Si una clase es abstracta no se puede heredar de ella.
    * TRUE, FALSE
    *
    * 3. Una clase abstracta tiene que tener métodos abstractos.Una clase puede ser abstracta y no tener métodos abstractos.
    * FALSE, TRUE
    *
    * 4. Si una clase tiene métodos abstractos tiene que ser abstracta.Una clase puede tener métodos abstractos y no ser abstracta.
    * TRUE, FALSE
    *
    * 5. Si una clase es abstracta sus subclases no pueden ser abstractas.Una clase abstracta puede tener subclases que también sean abstractas.
    * FALSE, TRUE
    *
    * 6. Si un método es abstracto en una clase, tiene que ser no abstracto enla subclase, o bien, la subclase tiene que ser también abstracta.Si un método es abstracto en una clase, no puede ser abstracto en lassubclases.
    * TREU, FALSE
    *
    * 7. Si un método se define final se tiene que reescribir en las subclases.Si un método se define final no se puede reescribir en las subclases.
    * FALSE, TRUE
    *
    * 8. Una clase puede tener un método final y no ser una clase finalSi una clase tiene un método final tiene que ser una clase final.
    * TRUE, FALSE
    *
    * 9. Si una clase se define final no se pueden definir subclases de ella.Si una clase se define final no se puede instanciar.
    * TREU, FALSE
    *
    * 10. Un método definido final y abstract resultaría inútil, puesto que nuncase podría implementar en las subclases.
    * Un método definido final y abstract podría resultar útil.
    *
    *
    * 11. Una clase definida final y abstract resultaría inútil, puesto que no sepodría instanciar ni heredar de ella.
    * Una clase definida final y abstract podría resultar útil.
    *
    *
    * */
}
